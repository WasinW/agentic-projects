# Session Context — VS Code, 2026-07-12 (everything EXCEPT cost-dashboard)

> **For:** สิน (วศิน) — Senior DE @ AIA. Portable brain-dump so a web/mobile chat can continue. The cost-dashboard sharing solution has its own doc (`aia-cost-dashboard-sharing-solution_20260712.md`); THIS file = the OTHER threads from today.
> **Discipline (important):** keep **AIA-actual** (confirmed real) SEPARATE from the **generic 3-part survey** (aspirational research). Don't attribute survey AI content to AIA. Air-gapped AIA machine → screenshots only, no code export from AIA.

---

## 1. What we did today (thread index)
1. Verified + **synced the Agent KB (Qdrant MCP)** from the MD files (was out of sync; Docker was off).
2. **Validated the 3-part solution-architecture survey** against Sin's real KB (NTT/SCB/AIA + roles) → fixed errors → folded into the `de-solution-architecture` skill.
3. **Corrected an AIA-vs-generic conflation** (web chat had presented the generic AI stack as AIA's).
4. Decoded the **full AIA data-platform architecture** from the org diagram → `aia/data-platform-architecture.md`.
5. Cost-dashboard sharing solution (separate doc).
6. Smaller: `az login`, `kubectl` for Strimzi, Kafka/AKS cost floors, local-Strimzi lab.

## 2. Sin's AIA scope = 3 workstreams (confirmed)
- **Producer** — Kafka/Strimzi/Debezium on AKS (the `dtp_kafka_*` repos). Original job.
- **ADB compute** — Azure Databricks Real-time + Batch clusters, with an Azure SQL MI "Framework DB" (`prd_frmwrk_db`). ⚠️ **CORRECTED 2026-07-13: the Framework DB is NOT the same as SCB's RDT config-driven framework** (Sin's correction — drop that analogy). Also **this workstream is DEPRIORITIZED for now** ("มี แต่อาจจะยังไม่ใช่ตอนนี้") — focus is the cost dashboard.
- **Cost-monitoring dashboard PoC** (separate doc).

## 3. Full AIA data-platform architecture (confirmed from org diagram)
`SOURCES → CDC (Qlik Replicate + Debezium) → EVENT PROCESSING (Strimzi Kafka on AKS + Connect + Grafana + Jenkins + ACR) → ADF orchestrate → COMPUTATION (Azure Databricks real-time + batch, config-driven via SQL MI Framework DB) → ADLS Gen2 (RAW→Persist→Staging: staging/adam/data-mart/HSM) → DATABASE (ODS on Azure SQL MI + Synapse Dedicated SQL pool: EDW/New QS/Departmental DM=Data Mart/UC=<team label, not Unity Catalog>/DGO=Data Governance Office) → SERVING (ESB/API→apps, PowerBI, SSMS) ; GOVERNANCE = Purview + Data 360 ; ANALYTIC = Databricks DS Lab + Departmental/Common/Amplify WS (client users, mlflow)`.
- **CDC = Qlik + Debezium** (two tools, not Debezium-only).
- **Departmental WS = workspaces where CLIENT users log in** (vs the internal DP WS). Some have Unity Catalog. DR design exists.
- Full detail: `aia/data-platform-architecture.md`.

## 4. The generic 3-part solution-architecture survey (research — NOT AIA)
We built + KB-validated a 3-layer architecture reference. Canonical = skill **`de-solution-architecture`**; deep docs = `producer-ingestion-survey_20260702.md` + `de-streaming-architecture-and-ai-survey_20260702.md`.
- **Part 1 Producer** (P1-P25): core decision = "do you even need a broker?"; Debezium Server / native CDC / Redpanda-WarpStream / SaaS as lighter options.
- **Part 2 Consumer+ETL** (A1-A16): core = "MERGE placement" (append-land raw → collapse in Silver); "kill 24×7" where compute bills per-second.
- **Part 3 ML/AI/agentic**: EXT-1 ML=Platinum, EXT-2 RAG=Diamond (embed from GOLD, trigger from CDC), EXT-3 agentic=Agent-State.
- **KB-validation caught real errors** (now fixed): The-1 producer = app-emitted events (NOT CloudRun/Debezium); AIA Debezium 1.9.7 (ROWID/drop-tx are 2.x, unusable now); "kill 24×7" villain is platform-specific (Dataflow worker is cheap; real cost = Pub/Sub hops + Bigtable IOPS + un-batched Iceberg commits); The-1 kept Beam for 5 reasons not just enrichment; the "3B-row scan" figure is illustrative not measured; Tecton = Databricks now.
- ⚠️ The survey's "AIA (Azure-Databricks)" column = **generic path IF extended into AI**, NOT AIA's real stack.

## 5. Agent KB sync mechanism (so you can do it yourself)
- KB = Qdrant (Docker, `localhost:6333`) + `_infra/embed_knowledge.py`. **If MCP won't connect → start Docker.**
- Full reindex `./_infra/reindex.sh` = SLOW (re-embeds all ~400 files). **Incremental** `python embed_knowledge.py --files <paths>` = fast.
- **zsh gotcha:** unquoted `$FILES` isn't word-split → use `find ... -print0 | xargs -0 python embed_knowledge.py --files`.
- Verify: `mcp list_files(prefix=...)` / `curl -s localhost:6333/collections/agent_knowledge | grep points_count`.

## 6. Smaller threads today
- **`az login` on the AIA machine:** `az login --tenant <AIA-tenant>` (browser) or `--use-device-code` (headless — but AIA conditional access may block, AADSTS530033); then `az account set -s <sub>`; for kubectl → `az aks get-credentials -g <rg> -n <cluster>`. Corp caveats: VPN, Intune device compliance, may need SPN.
- **kubectl for Strimzi** (once you have access): `kubectl get kafka,kafkaconnect,kafkaconnector,kafkatopic -n <ns>`; `kubectl describe kafkaconnector <n>`; `kubectl logs deploy/strimzi-cluster-operator`; connect pod logs `| grep -i debezium`. Full cheatsheet in skill `kafka-strimzi-cdc/reference/kubectl-cheatsheet.md`.
- **Kafka/AKS cost floor** (personal/dev): local **kind + Strimzi single-node = $0**; min prod-grade (3 broker KRaft, 1 env, small VM, reserved) ≈ **$300-500/mo**; dev VM ≈ $150/mo. AIA's ~$3-5k is driven by DR + multi-env + big VMs, not topics/storage.
- **Debezium 1.9.x connector anatomy** (AIA): `database.server.name` (not 2.x `topic.prefix`), `database.history.kafka.*` (not `schema.history.internal.*`); topics **auto-created** (no `KafkaTopic` CRs); connector-as-YAML onboarding via Jenkins→ACR→AKS, promote dev→uat→prod→prod-dr.

## 7. Docs created today (all in data-ml-ai-pipeline)
- `aia/data-platform-architecture.md` — full AIA architecture (confirmed).
- `knowledge_chat/aia-cost-dashboard-sharing-solution_20260712.md` — cost-dashboard solution + step-by-step.
- `knowledge_chat/context_20260712.md` — this file.
- Updated: skill `de-solution-architecture` (3-part validated), + correction banners on the two survey docs.
- Memory: `aia-new-job` (architecture + scope), `aia-cost-dashboard-poc`, `agent-kb-sync-mechanism`.

## 8. Open next actions
- Cost-dashboard: run `SELECT current_metastore()` in DP-batch WS + departmental WS → same metastore? → publish+RLS vs Delta Sharing. Verify view-shared-dashboard ≠ workspace-access. Then build proposal for พี่ Sarunya.
- ADB: Sin to explain the Databricks framework (SQL MI Framework DB) + what he'll help on → then deep-dive.
- Clarify Synapse EDW / New QS / Departmental-UC meanings.
