# local-rag-agent — 100% local, ไม่มี server
#
# ⚠️ Intel-mac (x86_64): PyTorch เลิก support หลัง torch 2.2.2
#    -> ต้อง pin ทั้ง stack ให้อยู่ยุคต้น 2024 ทั้งหมด ไม่งั้นชนกันเป็นทอด:
#       numpy 2 -> torch 2.2.2 พัง (_ARRAY_API not found)
#       scipy/sklearn ใหม่ -> ใช้ np.long ที่ numpy 1.26 ไม่มี
#       transformers ใหม่ -> เรียก torch API ที่ 2.2.2 ไม่มี
#    (ARM-mac / Linux ใหม่กว่า: ลบ pin ออก ใช้ล่าสุดได้เลย)
torch==2.2.2
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.2
transformers==4.44.2
sentence-transformers==3.0.1

pythainlp                # tokenizer ไทยให้ BM25 — ไม่ลง = ไทยถูกรวบเป็นก้อนเดียว
requests                 # เรียก Ollama /api/chat
fastmcp                  # (ง) MCP server SDK (FastMCP high-level) — python >=3.10
